import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

Loading.propTypes = {
    
};

function Loading(props) {
    return (
        <div>
            <h1>This is Loading of Component Loading</h1>
        </div>
    );
}

export default Loading;